#include <iostream>
#include <vector>
#include <algorithm>
#include <cmath>

using namespace std;

int main () {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int N, L, sum = 0;
    vector<int> input(L);
    vector<int> output(L);
    int max_val = L - 1;
    int min_val = -L;
    for (int i = 0; i < L; i++) {
        cin >> input[i];
    }
    for (int i = 0; i < L; i++) {
        
    }




}